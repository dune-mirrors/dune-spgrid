from setuptools import setup, find_packages

setup(name="dune.spgrid",
    namespace_packages=['dune'],
    description="Structured Parallel Grid for DUNE",
    version="3.0",
    author="Andreas Dedner and Martin Nolte",
    packages = find_packages(),
    zip_safe = 0,
    package_data = {'': ['*.so']}
  )
